@*@if (Model.BrowserRemembered)
    {

        <p>
            Browser is curently remembered for two factor:
            <input type="submit" value="Forget Browser" class="btn btn-default" />
        </p>
    }
    else
    {
        <p>
            Browser is curently not remembered for two factor:
            <input type="submit" value="Remember Browser" class="btn btn-default" />
        </p>
    }*@

@*if (Model.BrowserRemembered)
    {
        <form method="post" action="/Manage/ForgetBrowser" id="IsRememberBrowser">
            <p>
                Browser is curently remembered for two factor:
                <input type="submit" value="Forget Browser" class="btn btn-default" />
            </p>
        </form>
    }
    else
    {
        <form method="post" action="/Manage/RememberBrowser" id="IsRememberBrowser">
            <p>
                Browser is curently not remembered for two factor:
                <input type="submit" value="Remember Browser" class="btn btn-default" />
            </p>
        </form>
    }*@



@if (Model.BrowserRemembered)
{
    <form class="form-group row" method="post" action="/Manage/ForgetBrowser" id="IsRememberBrowser">
        <label class="col-sm-4 col-form-label">瀏覽器：</label>
        <div class="col-sm-8">
            <input type="submit" value="已記住" class="btn btn-success" />
        </div>
    </form>
}
else
{
    <form class="form-group row" method="post" action="/Manage/RememberBrowser" id="IsRememberBrowser">
        <label class="col-sm-4 col-form-label">瀏覽器：</label>
        <div class="col-sm-8">
            <input type="submit" value="已忘記" class="btn btn-danger" />
        </div>
    </form>
}



